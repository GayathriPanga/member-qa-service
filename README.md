# Member QA Service

A FastAPI-based question answering system that infers answers from member messages.

## How to run locally
pip install -r requirements.txt
uvicorn app:app --reload

More details coming soon.
